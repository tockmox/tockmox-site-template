# This is a TEMPLATE. It must stay safe to make public.
# Real secrets never live here as plaintext ... only SealedSecrets, which are
# encrypted to your cluster key and safe to commit.
*.tgz
.DS_Store

# Never commit an unsealed secret or a kubeconfig.
*.kubeconfig
kubeconfig
